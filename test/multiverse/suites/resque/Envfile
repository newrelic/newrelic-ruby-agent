gemfile <<-RB
  gem 'redis', '< 4.0.0' if RUBY_VERSION < '2.2.0'
  gem 'sinatra', '< 2.0' if RUBY_VERSION < '2.2.0'
  gem 'resque', '1.23.0'
  gem 'json'
RB

gemfile <<-RB
  gem 'redis', '< 4.0.0' if RUBY_VERSION < '2.2.0'
  gem 'sinatra', '< 2.0' if RUBY_VERSION < '2.2.0'
  gem 'resque', '~>1.25.1'
  gem 'json'
RB
