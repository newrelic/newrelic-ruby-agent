suite_condition("gRPC not compatible with Ruby < 2.5") do
  RUBY_VERSION >= '2.5.0'
end

instrumentation :chain, :prepend

GRPC_VERSIONS = [
  nil
]

GRPC_VERSIONS.each do |version|
  gemfile <<-RB
    gem 'grpc'#{version}
  RB
end

# vim: ft=ruby
