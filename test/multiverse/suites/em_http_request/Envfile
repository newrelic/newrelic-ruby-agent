em_http_request_versions = %w(1.1.1 1.1.2)

em_http_request_versions.each do |em_http_request_version|
  gemfile <<-RB
    gem 'em-http-request', '~> #{em_http_request_version}'
    gem 'rack'
    gem 'json', :platforms => [:rbx, :mri_18]
  RB
end
