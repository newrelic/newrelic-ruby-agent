#!/usr/bin/bash

# test_all - Test all Rubies. Run 'bundle install' and 'bundle rake test:all'
#            against all installed rubies
#
#            usage: test_all

for v in $(find /root/.rubies/*/bin -name bundle); do
  cd /app
  $v install
  $v exec rake test:all
done
