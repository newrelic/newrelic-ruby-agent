name: 'Hello World'
description: 'Greet someone and record the time'
inputs:
  ruby-version:
    description: 'Version of Ruby we want to use'
    required: true
    default: '2.0.0-p648'
outputs:
  results: 
    description: 'The output!'
runs:
  using: 'node12'
  main: 'index.js'