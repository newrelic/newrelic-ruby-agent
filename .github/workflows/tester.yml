name: Testing
on:
  pull_request:
  workflow_dispatch:

jobs:
  set_vars:
    runs-on: ubuntu-22.04
    steps:
      - run: echo "thekey=theval" >> $GITHUB_ENV

  use_vars:
    runs-on: ubuntu-22.04
    steps:
      - run: echo "thekey's value is ${{vars.thekey}}"
