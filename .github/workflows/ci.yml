name: Continuous Integration

on:
  push:
    branches:
      - exec-tester
  pull_request:

jobs:
  exec-tester:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2

      - name: Test exec
        uses: ./.github/actions/exec-tester
