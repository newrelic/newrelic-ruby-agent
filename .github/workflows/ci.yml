name: PR Continuous Integration
on:
  pull_request:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        run: echo "Hello, World - test!"

  issue_closer:
    if: github.event.pull_request.merged
    runs-on: ubuntu-latest
    steps:
      - name: issue_closer Action
        uses: ./actions/issue_closer # no tag - locally sourced

